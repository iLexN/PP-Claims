{% extends "template/general.twig" %}

{% block mainHTML %}

    <div class="row">
        <div class="col s12 m10 l8 offset-m1 offset-l2">
            <div class="row">
                <div class="col s12"><p>Please Select service:</p></div>
                <div class="col s12 m6 l6">
                    {{ components.main_block_effect(langText.title_profile,langText.desc_profile,'#1') }}
                </div>
                <div class="col s12 m6 l6">
                    {{ components.main_block_effect(langText.title_claims,langText.desc_claims,'#2') }}
                </div>
                <div class="col s12 m6 l6">
                    {{ components.main_block_effect(langText.title_policy,langText.desc_policy,'#3') }}
                </div>
                <div class="col s12 m6 l6">
                    {{ components.main_block_effect(langText.title_contact,langText.desc_contact,'#3') }}
                </div>
               <div class="col s12"><p>Please Select service:</p></div>
            </div>
        </div>
    </div>
{% endblock %}

{% block jsBottom %}
    {{ parent() }}
    <script>
        // this will increase the api server loading
        $.get({
            url : "/ajax/preload",
            global : false
        });
    </script>
{% endblock %}
